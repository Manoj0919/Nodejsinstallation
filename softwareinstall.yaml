-hosts: all
become: true
vars:
  
tasks:
  -name: 
  -name: install java
    apt:
      name: maven
      state: true
  -name: InstallNginx
    apt: 
      name: nginx
      state: true
  tags:
   - Installnginx
  -name: start Nginx


